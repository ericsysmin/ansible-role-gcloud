---
sudo: required
language: python
services:
  - docker
before_install:
  - sudo apt-get -qq update
install:
  - pip install molecule
  - pip install docker
  - mv /home/travis/build/ericsysmin/ansible-role-gcloud /home/travis/build/ericsysmin/ericsysmin.gcloud
script:
  - molecule test
