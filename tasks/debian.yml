---
# tasks that install gcloud on debian
- name: gcloud | Add an Apt signing key, uses whichever key is at the URL
  apt_key:
    url: https://packages.cloud.google.com/apt/doc/apt-key.gpg
    state: present

- name: gcloud | Add the gcloud repository
  apt_repository:
    repo: "deb http://packages.cloud.google.com/apt cloud-sdk-{{ ansible_distribution_release }} main"
    state: present
    filename: google-cloud-sdk

- name: gcloud | Install the google-cloud-sdk package
  apt:
    name: google-cloud-sdk
    update_cache: yes
